package gmod;

import haxe.ds.Option;

class CustomEntTools {

    // public static function toHaxe<T:gmod.sent.ENT>(ent:Entity,c:Class<T>):T {
    //     return cast untyped ent._gHaxeBurrow;
    // }

    // public static function toHaxeSafe<T:gmod.sent.ENT>(ent:Entity,c:Class<T>):Option<T> {
    //     var getEnt:T = untyped ent._gHaxeBurrow;
    //     if (getEnt != null && Std.is(getEnt,c)) {
    //         return Some(cast untyped ent._gHaxeBurrow);
    //     } else {
    //         return None;
    //     }
    // }
}