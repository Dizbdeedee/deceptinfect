package deceptinfect.ents;

import gmod.sent.SentBuild;

class Di_weaponcache extends gmod.sent.ENT_ANIM implements SentBuild {

    final properties:EntFields = {
        Base: "base_anim"
    };

    

}