package deceptinfect.ents;

import gmod.sent.SentBuild;

class Di_flare extends gmod.sent.ENT_ANIM implements SentBuild {
    final properties:SentBuild.EntFields = {
        Base = "base_entity";
    }

    override function Initialize() {
        
    }
}